import React from "react";

const Man = (props) => {
    return(
        <div className="Man">
        <h1>{props.count}This is Functional base Component</h1>
        </div>
    );
}

export default Man;